import { Baby, ClipboardCheck, HouseHeart, Star, User, type LucideIcon } from "lucide-react"


const IconMap: Record<string, LucideIcon> = {
    User,
    Baby,
    HouseHeart,
    ClipboardCheck,
    Star,
}

export default IconMap